double area_cuadrado(double lado) { return lado * lado; }

double area_triangulo(double base, double altura) {
  return base * altura / 2.0;
}

double perimetro_cuadrado(double lado) { return 4 * lado; }
